package org.robotlegs.signals
{
	import org.osflash.signals.Signal;

	/**
	 * The <code>ViewController</code> listens for this signal for adding the view to stage. 
	 * 
	 * @author Peder A. Nielsen
	 * @version 1.0
	 */
	public class AddViewSignal extends Signal
	{
		public function AddViewSignal()
		{
			super();
		}
	}
}
